<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show</title>
  </head>
  <body>
    <%= image_tag @photo.image.url(:large) %>
    <%= @photo.caption %>
    <a href="/photos/<%= @photo.id %>/comments/new">Add comment</a>
  </body>
</html>
